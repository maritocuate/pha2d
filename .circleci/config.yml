version: 2.1

jobs:
  saluda:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo hola
  despidete:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo adios

workflows:
  wf1:
    jobs:
      - saluda
      - despidete
  wf2:
    jobs:
      - saluda
      - despidete:
        requires:
          - saluda
